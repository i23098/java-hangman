<?xml version="1.0" encoding="UTF-8"?>
<Context>
    <Resource name="jdbc/hangmanDS"
              type="javax.sql.DataSource"
              auth="Container"
              driverClassName="org.postgresql.Driver"
              url="jdbc:postgresql://${db.host}:${db.port}/${db.hangman}"
              username="${db.hangman.username}"
              password="${db.hangman.password}"
              maxTotal="20"
              maxIdle="10"
              maxWaitMillis="-1"/>
</Context>